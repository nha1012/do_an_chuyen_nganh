import { HttpClient } from '@angular/common/http';
import { ResponseLogin } from './interfaces/auth.interface';
import { DTGAuthModuleOptions } from './dtg-auth.config';
import { ApplicationEntity } from './interfaces';
import { Observable } from 'rxjs';
import * as ɵngcc0 from '@angular/core';
export declare class DTGAuthService {
    private http;
    private config;
    constructor(http: HttpClient, config: DTGAuthModuleOptions);
    authenticate(params: {
        username: string;
        password: string;
        appId?: string;
    }): Promise<ResponseLogin>;
    isAuthenticate(): boolean;
    getToken(): string;
    getInfo(): any;
    deAuthenticate(): void;
    isAccess(params: {
        appId: string;
    }): Observable<boolean>;
    getAppInfo(params: {
        appId: string;
        isGuest?: boolean;
    }): Observable<ApplicationEntity>;
    static ɵfac: ɵngcc0.ɵɵFactoryDef<DTGAuthService, never>;
}

//# sourceMappingURL=ditagis-auth.service.d.ts.map